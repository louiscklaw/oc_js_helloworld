# task1

## purpose

imagine you are on the directory `project/src`

please read `main.py` and modify printing `helloworld1` to `apple`


## Hwo to do it

jod down and keep track your task to do in `tmp/TODO.md`.


## steps before and after the change(s)
please run `./test.sh` to check the result before you edit, pipe the output to `tmp/test_before.out`

please run `./test.sh` to check the result you edit, pipe the output to `tmp/test_after.out`

compare `tmp/test_before.out` and `tmp/test_after.out` to see if the change desired.

when you find it is good to commit, perform `git commit`

## commit message

a simple message to log your change is good enough.


thanks.
