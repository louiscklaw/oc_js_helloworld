# "new_ticket" Command

When the user says "create_new_ticket" with the content.

you will first read the currnt `*.md` files in:
`./tasks/_lib`
`./tasks/_parking`

with your context, merge to the context in the converation. create a new markdown file with suggested name at `_queue` folder

